import React from 'react';

class ResultServer extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <span>ResultServer</span>
    );
  }
}

export default ResultServer;
